"use strict"; // treat all JS code as newer version

const accId = 77519; // only way to declare a constant
let accEmail = "vna.vivek.official@gmail.com";
var accPass = 97192; // let and var can be used to declare variables
let city = "Kanpur"; // don't use var tho....
country = "India"; // can be declared like this too
let pincode; // variable declaration

// accId=3478        // wont change
console.log(accId + 9);
console.table([accEmail, accPass, city, pincode]);
